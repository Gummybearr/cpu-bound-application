config:
  target: "http://35.222.16.183/"
  phases:
    - duration: 120
      arrivalRate: 40
      name: Warm up
scenarios:
  # We define one scenario:
  - name: "just get hash"
    flow:
      - get:
          url: "/hash/123"