config:
  target: "http://35.202.247.68"
  phases:
    - duration: 360
      arrivalRate: 16
      name: Warm up
scenarios:
  # We define one scenario:
  - name: "just get hash"
    flow:
      - get:
          url: "/hash/123"